plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.3.70'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    jcenter()
}


configurations {
    embed
    // Make compile extend from our provided configuration so that things added to bundled end up on the compile classpath
    compile.extendsFrom(embed)
}


dependencies {
    embed "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
    embed "net.dv8tion:JDA:4.1.1_115"
    embed "io.ktor:ktor-client:1.3.1"
    embed "io.ktor:ktor-client-core:1.3.1"
    embed "io.ktor:ktor-client-websockets:1.3.1"
    embed "io.ktor:ktor-client-cio:1.3.1"
    embed "com.google.code.gson:gson:2.8.6"
    embed "org.slf4j:slf4j-simple:1.7.30"
}

jar {
    // Include all of the jars from the bundled configuration in our jar
    from configurations.embed.collect {
        exclude 'META-INF/LICENSE'
        exclude 'LICENSE.txt'
        exclude 'META-INF/LICENSE.txt'
        it.isDirectory() ? it : it.path.endsWith(".pom") ? null : zipTree(it)
    }
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}